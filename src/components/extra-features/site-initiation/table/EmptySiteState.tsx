
import React from 'react';

export const EmptySiteState: React.FC = () => {
  return (
    <div className="text-center py-10 text-muted-foreground">
      No sites found. Upload a CSV to import site data.
    </div>
  );
};
